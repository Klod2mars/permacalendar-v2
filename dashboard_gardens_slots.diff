diff --git a/lib/shared/widgets/organic_dashboard.dart b/lib/shared/widgets/organic_dashboard.dart
index 1111111..2222222 100644
--- a/lib/shared/widgets/organic_dashboard.dart
+++ b/lib/shared/widgets/organic_dashboard.dart
@@
 import 'package:go_router/go_router.dart';
+
+import '../../features/garden/providers/garden_provider.dart';
+import '../../core/models/garden_freezed.dart';
@@
   // Defaults (migrated from previous static hotspots)
   static const List<_Hotspot> _hotspots = <_Hotspot>[
-    _Hotspot(id: 'intelligence', centerX: 0.18, centerY: 0.22, widthFrac: 0.20, heightFrac: 0.20, route: AppRoutes.intelligence, label: 'Intelligence'),
-    _Hotspot(id: 'calendar', centerX: 0.18, centerY: 0.50, widthFrac: 0.20, heightFrac: 0.20, route: AppRoutes.calendar, label: 'Calendar'),
-    _Hotspot(id: 'activities', centerX: 0.18, centerY: 0.78, widthFrac: 0.20, heightFrac: 0.20, route: AppRoutes.activities, label: 'Activities'),
-    _Hotspot(id: 'weather', centerX: 0.50, centerY: 0.18, widthFrac: 0.18, heightFrac: 0.18, route: AppRoutes.weather, label: 'Weather'),
-    _Hotspot(id: 'gardens', centerX: 0.65, centerY: 0.28, widthFrac: 0.42, heightFrac: 0.36, route: AppRoutes.gardens, label: 'Gardens'),
+    _Hotspot(id: 'intelligence', centerX: 0.18, centerY: 0.22, widthFrac: 0.20, heightFrac: 0.20, route: AppRoutes.intelligence, label: 'Intelligence'),
+    _Hotspot(id: 'calendar', centerX: 0.18, centerY: 0.50, widthFrac: 0.20, heightFrac: 0.20, route: AppRoutes.calendar, label: 'Calendar'),
+    _Hotspot(id: 'activities', centerX: 0.18, centerY: 0.78, widthFrac: 0.20, heightFrac: 0.20, route: AppRoutes.activities, label: 'Activities'),
+    _Hotspot(id: 'weather', centerX: 0.50, centerY: 0.18, widthFrac: 0.18, heightFrac: 0.18, route: AppRoutes.weather, label: 'Weather'),
+    // five small garden bubbles (static hotspots)
+    _Hotspot(id: 'garden_1', centerX: 0.60, centerY: 0.36, widthFrac: 0.15, heightFrac: 0.15, route: null, label: 'Garden 1'),
+    _Hotspot(id: 'garden_2', centerX: 0.68, centerY: 0.44, widthFrac: 0.15, heightFrac: 0.15, route: null, label: 'Garden 2'),
+    _Hotspot(id: 'garden_3', centerX: 0.54, centerY: 0.52, widthFrac: 0.15, heightFrac: 0.15, route: null, label: 'Garden 3'),
+    _Hotspot(id: 'garden_4', centerX: 0.70, centerY: 0.60, widthFrac: 0.15, heightFrac: 0.15, route: null, label: 'Garden 4'),
+    _Hotspot(id: 'garden_5', centerX: 0.60, centerY: 0.68, widthFrac: 0.15, heightFrac: 0.15, route: null, label: 'Garden 5'),
+    // settings square hotspot
+    _Hotspot(id: 'settings_square', centerX: 0.82, centerY: 0.90, widthFrac: 0.20, heightFrac: 0.12, route: AppRoutes.settings, label: 'Paramètres'),
   ];
@@
       final Map<String, String> _routeMap = {for (final h in OrganicDashboardWidget._hotspots) h.id: h.route};
+      // allow null route entries
+      final Map<String, String?> _routeMap = {for (final h in OrganicDashboardWidget._hotspots) h.id: h.route};
@@
-                final side = (cfg.size * (width < height ? width : height)).clamp(8.0, (width < height ? width : height));
+                final side = (cfg.size * (width < height ? width : height)).clamp(48.0, (width < height ? width : height));
@@
 class _Hotspot {
   const _Hotspot({
     required this.id,
     required this.centerX,
     required this.centerY,
     required this.widthFrac,
     required this.heightFrac,
-    required this.route,
+    this.route,
     this.label,
   });
@@
   final double widthFrac;
   final double heightFrac;
-  final String route;
+  final String? route;
   final String? label;
@@
     // Normal behaviour: tap to navigate
-    return _HotspotButton(
-      onTap: () {
-        if (kDebugMode) debugPrint('OrganicDashboard: tapped calibrated hotspot (${widget.id}) -> ${widget.onTapRoute}');
-        if (widget.onTapRoute != null) context.push(widget.onTapRoute!);
-      },
-      semanticLabel: widget.cfg.id,
-      showDebugOutline: widget.showDebugOutline,
-    );
+    return _HotspotButton(
+      onTap: () async {
+        if (kDebugMode) debugPrint('OrganicDashboard: tapped calibrated hotspot (${widget.id}) -> ${widget.onTapRoute}');
+        if (widget.id.startsWith("garden_")) {
+          await _handleGardenTap(context);
+          return;
+        }
+        if (widget.onTapRoute != null) context.push(widget.onTapRoute!);
+      },
+      semanticLabel: _semanticLabelFor(widget.cfg.id),
+      showDebugOutline: widget.showDebugOutline,
+    );
+
+  String _semanticLabelFor(String id) {
+    if (id.startsWith("garden_")) {
+      final m = RegExp(r'garden_(\d+)').firstMatch(id);
+      return 'Garden ${m?.group(1) ?? ""}';
+    }
+    if (id == "settings_square") return "Paramètres";
+    return id;
+  }
+
+  Future<void> _handleGardenTap(BuildContext context) async {
+    final slotId = widget.id;
+    try {
+      final gardenState = widget.ref.read(gardenProvider);
+      GardenFreezed? found;
+      for (final g in gardenState.gardens) {
+        if ((g.metadata["dashboard_slot"] ?? "") == slotId) {
+          found = g;
+          break;
+        }
+      }
+
+      if (found != null) {
+        context.push("/gardens/${found.id}");
+        return;
+      }
+
+      final canAdd = widget.ref.read(canAddGardenProvider);
+      if (!canAdd) {
+        ScaffoldMessenger.of(context).showSnackBar(const SnackBar(content: Text("Limite de 5 jardins atteinte")));
+        context.push(AppRoutes.gardens);
+        return;
+      }
+
+      final n = RegExp(r'garden_(\d+)').firstMatch(slotId)?.group(1) ?? "";
+      final newGarden = GardenFreezed.create(name: "Garden $n", metadata: {"dashboard_slot": slotId});
+      final success = await widget.ref.read(gardenProvider.notifier).createGarden(newGarden);
+      if (!success) {
+        ScaffoldMessenger.of(context).showSnackBar(const SnackBar(content: Text("Erreur lors de la création du jardin")));
+        return;
+      }
+
+      final updatedState = widget.ref.read(gardenProvider);
+      final created = updatedState.gardens.firstWhere(
+        (g) => (g.metadata["dashboard_slot"] ?? "") == slotId,
+        orElse: () => newGarden,
+      );
+      context.push("/gardens/${created.id}");
+    } catch (e) {
+      if (kDebugMode) debugPrint("Error handling garden tap: $e");
+      ScaffoldMessenger.of(context).showSnackBar(SnackBar(content: Text("Erreur: $e")));
+    }
+  }
diff --git a/lib/core/providers/organic_zones_provider.dart b/lib/core/providers/organic_zones_provider.dart
index 3333333..4444444 100644
--- a/lib/core/providers/organic_zones_provider.dart
+++ b/lib/core/providers/organic_zones_provider.dart
@@
     final positions =
         await PositionPersistence.load('organic', defaultPositions);
     final sizes = await PositionPersistence.loadSizes('organic', defaultSizes);
     final flags =
         await PositionPersistence.loadBooleans('organic', defaultEnabled);
+
+    // Migration: single "gardens" key -> 5 garden_n keys
+    try {
+      if (!positions.containsKey("garden_1")) {
+        final old = await PositionPersistence.readPosition("organic", "gardens");
+        if (old != null) {
+          if (kDebugMode) debugPrint("🔧 [CALIBRATION] migrating organic.gardens -> garden_1..5");
+          final base = Offset((old["x"] as double), (old["y"] as double));
+          final oldSize = (old["size"] as double?) ?? 0.25;
+          final enabled = (old["enabled"] as bool?) ?? true;
+
+          final offsets = [
+            Offset(-0.06, -0.02),
+            Offset(0.06, -0.02),
+            Offset(-0.06, 0.06),
+            Offset(0.06, 0.06),
+            Offset(0.0, 0.12),
+          ];
+
+          final newPositions = <String, Offset>{};
+          final newSizes = <String, double>{};
+          final newFlags = <String, bool>{};
+
+          for (var i = 0; i < offsets.length; i++) {
+            final id = "garden_${i + 1}";
+            final p = Offset(
+              (base.dx + offsets[i].dx).clamp(0.0, 1.0),
+              (base.dy + offsets[i].dy).clamp(0.0, 1.0),
+            );
+            newPositions[id] = p;
+            newSizes[id] = (oldSize * 0.6).clamp(0.12, 0.20);
+            newFlags[id] = enabled;
+            positions[id] = p;
+            sizes[id] = newSizes[id]!;
+            flags[id] = enabled;
+          }
+
+          await PositionPersistence.save("organic", newPositions);
+          await PositionPersistence.saveSizes("organic", newSizes);
+          await PositionPersistence.saveBooleans("organic", newFlags);
+        }
+      }
+    } catch (e) {
+      if (kDebugMode) debugPrint("🔧 [CALIBRATION] migration failed: $e");
+    }

